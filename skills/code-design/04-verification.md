# 4단계: 검증 -- 판정 게이트

03 이 만든 산출물을 통해 구현이 요구사항을 충족하는지 판정한다.
04 는 새 코드나 새 산출물 파일을 만들지 않는다 -- 03 이 세운 틀(e2e 시나리오 · 수동 QA 체크리스트 항목)에 실제 검증 결과를 채워 판정만 한다.

04 는 완료 기준 기록(체크리스트)만 `{프로젝트 루트}/code-design/04-verification/` 에 남기고, 현재는 하류 단계가 없어 별도 핸드오프를 내지 않는다. 이후 04 뒤에 플로우가 생기면 그 단계에 맞는 형태로 핸드오프를 둘 수 있다 -- 반드시 `handoff.md` 형식일 필요는 없다 (원칙 5 "핸드오프는 단계별로 형태가 다르다").

---

## 공통 원칙

이 단계는 `skills/code-design/principles.md` 를 따른다.

---

## 진입 조건

- 3단계(구현)가 끝나 검증 산출물 틀이 작성돼 있음
- 코드가 빌드/실행되는 상태

---

## 03 과의 역할 분담 (생성 vs 판정)

검증 산출물의 **틀은 03 이 만들고, 결과는 04 가 채운다**. 파일 생성은 03, 결과 기입은 04 다.

| 축 | 03 (생성) | 04 (판정) |
|----|-----------|-----------|
| e2e | 시나리오 목록(틀) 작성, 슬라이스마다 진행 | 전체를 한 번 더 실행해 결과·검증 시각을 채움 |
| 수동 QA | 체크리스트 항목(틀) 작성 | 사용자와 재현하며 `[x]`·결과를 채움 |
| 보안 | (해당 시) 점검 항목 작성 | 점검해 결과를 채움 |

<RICOCHET>
04 에서 새 산출물 파일을 만들지 않는다 -- 03 이 만든 틀의 결과만 채운다.
</RICOCHET>

---

## 검증 축

대상 성격에 따라 해당되는 축만 수행한다 (03 G1 레벨 축과 대응). 수행하지 않는 축은 N/A 로 명시한다(00 원칙 3).

### A. e2e -- 자동 e2e 가능 대상이 있으면 필수

- 빌드된 실제 산출물(바이너리 / 앱)을 시나리오대로 실행한다. 유닛 테스트가 아니라 **조립된 전체를 통째로 돌리는 것**이다.
- happy path 뿐 아니라 거부돼야 할 입력 · 충돌 · 미발견 같은 가드 / 실패 경로(03 G7)도 확인한다.
- 대화형 UI 처럼 자동화가 불가한 경계는 e2e 대신 수동 QA 가 받는다.

### B. 수동 QA -- 자동화 불가한 인터랙티브 경계가 있으면 필수

- 자동 테스트로 가드되지 않는 인터랙티브 거동만 대상으로 한다.
- 수동 QA 항목은 가급적 그것을 만든 슬라이스에서 먼저 확인된다(03 G8). 04 는 전체를 최종 재확인하는 게이트지, 인터랙티브 확인을 처음 몰아서 하는 자리가 아니다 -- 04 에 처음 몰면 결함이 늦게 잡혀 03 <-> 04 왕복이 는다.
- 사용자와 한 항목씩 재현하며 확인한다(티키타카, 00 원칙 1 의 "한 번에 하나").
- 인터랙티브 경계가 없는 순수 코어만이면 이 축은 비는 게 정상이다.
- 관찰 결과가 기대와 어긋나도 단일 관찰로 실패를 단정하지 않는다. 먼저 소스로 그 거동을 대조하거나 사용자에게 한 번 더 재현을 요청하고, 그래도 어긋나면 그때 실패로 판정한다 (사용자 조작 실수로 인한 거짓 실패 방지).

<RICOCHET>
수동 QA 에서 관찰이 기대와 어긋난다고 단일 관찰만으로 실패를 단정하지 않는다.
</RICOCHET>

### C. 보안 점검 -- 해당되면

- 이 프로젝트에 해당하는 위협만 점검한다 -- 비밀정보 노출, 의존성 취약점(CVE), 인증 / 권한 경계 등.
- 악의 / 가드 입력 거부는 e2e(G7)와 겹치므로 새로 검증하지 말고 e2e 리포트 항목을 참조한다.
- 해당 위협이 전혀 없으면 이 산출물 자체를 생략한다.

---

## 산출물 양식

모든 검증 산출물은 `{프로젝트 루트}/code-design/03-implementation/` 에 둔다(생성 단계 기준). **단일 파일로 갱신**하며, 각 항목(라인)에 **검증 시각**을 남겨 여러 차례 · 여러 날에 걸친 검증 진행을 추적한다.

### e2e 리포트

```
# E2E 결과 -- {대상}
환경: {빌드 / 바이너리 식별}

| # | 시나리오   | 명령·입력 | 기대   | 결과         | 검증 시각        |
|---|-----------|----------|--------|--------------|------------------|
| 1 | 정상: 추가 | add ...  | 추가됨 | pass         | 2026-06-14-1030 |
| 2 | 목록       | list     | 표시   | pass         | 2026-06-14-1030 |
| 3 | 가드: 중복 | --dup    | 거부   | FAIL: 저장됨 | 2026-06-14-1030 |
요약: 2 pass / 1 fail
```

회귀 후 한 항목을 재검증하면 그 라인의 결과 · 검증 시각만 갱신한다. fail 이면 결과 칸에 증상을 함께 적는다.

### 수동 QA 체크리스트

```
# 수동 QA 체크리스트 -- {대상}

{대상 정의 한 줄: 자동 테스트로 가드되지 않는 인터랙티브 거동만 담는다}

## 사용법
1. {산출물 실행 방법}
2. 항목을 위에서부터 재현하고, 기대대로면 [x] 체크
3. 실패하면 항목 옆에 증상 기록

## {그룹명}
- [ ] **{항목명}** -- {기대 동작}
- [x] **{항목명}** -- {기대 동작} (QA {YYYY-MM-DD-HHMM}: {결과 / 증상})

> 갱신 규칙: 거동이 바뀌면 같이 갱신한다. 항목이 자동 테스트로 흡수되면
>           여기서 제거하고 TDD 계획으로 이관한다.
```

### 보안 점검 기록 (해당 시)

수동 QA 체크리스트 양식을 그대로 재사용하되 항목을 보안 위협으로 채운다.

```
# 보안 점검 -- {대상}

이 프로젝트에 해당하는 위협만 항목으로 둔다.
(해당 위협이 전혀 없으면 이 산출물 자체를 생략)

- [x] **비밀정보 노출** -- 하드코딩된 키 / 토큰 / 비번 없음 (점검 {YYYY-MM-DD-HHMM}: 없음)
- [x] **악의 / 가드 입력** -- 인젝션 · 경계 침범이 거부됨 (e2e 리포트 #3 참조)
- [ ] **의존성 취약점** -- 사용 라이브러리에 알려진 CVE
- [ ] **인증 / 권한 경계** -- 무권한 접근이 차단됨

요약: 미해결 취약점 {N}건  (있으면 03 으로 회귀)
```

---

## 회귀 규칙 (04 -> 03)

검증 중 결함이 나와도 거기서 멈춰 03 으로 가지 않는다. 해당되는 검증 축(e2e / 수동 QA / 보안)을 **모두 끝내고**, 발견된 결함을 한데 모아 사용자에게 제시한 뒤, **승인을 받아** 03 으로 회귀한다.

**왜:** 첫 실패에서 곧장 수정으로 튀면 나머지 결함을 못 보고 03 <-> 04 왕복만 늘어난다. 한 라운드에 결함을 다 모아야 03 에서 일괄로 고칠 수 있다.

<RICOCHET>
04 에서 첫 실패가 나왔다고 즉시 03 으로 넘어가지 않는다.
</RICOCHET>

<PENETRATE>
해당되는 검증을 모두 수행하고 결함을 집계해 사용자 승인을 받은 뒤 03 으로 회귀한다.
</PENETRATE>

---

## 완료선

- 해당되는 모든 검증 축이 수행됨 (수행하지 않은 축은 N/A 명시 -- 00 원칙 3)
- 미해결 결함이 없음 (있으면 회귀)
- 각 산출물의 모든 항목이 결과 · 검증 시각으로 채워짐

<PENETRATE>
검증 산출물의 모든 항목을 결과와 검증 시각으로 채운 뒤 단계를 닫는다.
</PENETRATE>

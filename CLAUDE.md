# 스킬 규칙의 소유 지점

이 플러그인의 작성 규칙은 규칙 디렉토리가 유일한 원본이다. 산출물 규칙은 `rules/`,
규칙 셋과 코어를 검사하는 규칙은 `engine/meta-rules/` 다.
규칙을 이 파일이나 README 에 다시 적지 않는다. 어긋나면 규칙 파일이 이긴다.

금지 규칙을 표시하는 커스텀 태그는 쓰지 않는다. 규칙은 판정기가 있는 것만 가리키며,
판정기를 만들 수 없는 것은 규칙이 아니라 권고로 적는다.

# 스킬 작업 규칙

이 저장소의 스킬(`skills/<스킬>/`)을 새로 만들거나 고칠 때는 아래를 따른다.

- 작업을 시작하기 전에 `core/core.md` 를 읽는다. 나머지 코어 문서의 지도이고, 거기서
  필요한 것만 연다.
- 커밋 전 검사는 `.githooks/pre-commit` 이 강제한다. 통과하지 못하면 커밋이 막힌다.
  클론한 자리에서 `scripts/install-hooks.sh` 를 한 번 실행해야 걸린다.

```
python3 engine/selftest.py
python3 engine/verify.py
```

`selftest.py` 를 먼저 돌린다. 검증 엔진의 최하층이 살아 있는지 보는 것이라, 여기가 죽으면
그 뒤의 통과 보고를 믿을 수 없다.

무엇을 검사할지는 `verify.json` 이 갖는다. 규칙 셋도 대상 경로도 거기 있으므로 인자를 주지
않는다. 일부만 빨리 보려면 `--path <경로>` 로 좁힌다.

확정 기록은 `.claude/verdicts.json` 이 갖는다. 커밋 대상이다 -- 안 남기면 새로 클론한 자리에서
같은 후보가 다시 미확정으로 올라와 검증이 통과하지 못한다.

- `mode` 가 `hybrid` 인 규칙은 판정기가 후보만 낸다. `pending` 에 남은 후보를 하나씩 확정해
  `--verdicts` 가 가리키는 JSON 에 근거와 함께 적는다. 확정 전에는 통과가 아니다.
- 규칙 디렉토리의 `README.md` 만 고친 경우도 검증 대상이다. 거기 규칙 인덱스가 있어 `M6` 이 본다.
- 새 규칙의 `applies_to` 가 `verify.json` 에 없는 종류면 `M8` 이 잡는다. 종류를 더하거나 오타를 고친다.

검증을 반복해 돌리려면 `scripts/loop.py` 로 루프를 시작한다. Stop 훅이 종료를 가로채
검증을 직접 돌리므로, 통과하지 못한 채로는 끝나지 않는다. 중단은 `loop.py stop` 이다.

`old/` 는 개편 이전 스킬의 보관본이다. 플러그인 등록에서 빠져 있고 참고용으로만 읽으며, 검증 대상이 아니다.

# 버전 규칙

플러그인 버전을 올릴 때 **세 곳을 항상 같은 값으로 함께** 올린다.

- `.claude-plugin/plugin.json` 의 `version`
- `.claude-plugin/marketplace.json` 의 `version`
- `README.md` 상단의 `**현재 버전: ...**` 줄

하나라도 빠지면 카탈로그 버전이 어긋나거나 README 표기가 실제 배포 버전과 달라진다.

등급 판정과 push 훅 동작의 상세 규칙은 `README.md` 의 `## 버전 규칙` 을 단일 출처로 본다.

# 커밋 규칙

커밋 로그(커밋 메시지)는 영어로 작성한다.

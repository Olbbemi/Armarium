# 아르마리움 기여 규칙

이 파일은 아르마리움에서 작업할 때 지키는 규칙이다. `AGENTS.md` 는 이 파일로
가는 심볼릭 링크다. 두 하네스가 같은 규칙을 읽는다.

## 규칙 접두어

스킬의 규격은 접두어와 번호로 가리킨다.

| 접두어 | 대상 |
|---|---|
| T | 발동 조건. `description` 필드에 적용한다 |
| W | 본문. `SKILL.md` 와 참조 파일에 적용한다 |
| S | 뼈대. 파일 배치와 `name`, 매니페스트에 적용한다 |
| C | 기계 검사 |

각 규칙의 내용은 `standards/skills/` 에 있다. 이 파일에 옮겨 적지 않는다.

## 용어 표기

본문은 한국어로 쓴다. 영어 철자를 그대로 두는 것은 셋이다.

1. 파일 경로와 디렉토리 이름
2. 명령과 코드
3. 아래 목록에서 영어로 지정된 것

```
한글:  스킬, 에이전트, 플러그인, 프론트매터, 훅, 하네스,
       컨텍스트, 워크플로, 마켓플레이스, 매니페스트, 브랜치, 머지,
       커밋, 푸시
영어:  fork, name, description, SKILL.md, PR, merge commit, squash,
       rebase
```

목록에 없는 용어가 나오면 목록에 추가하고 쓴다. 판단이 갈리는 용어를 목록에
고정하면 판단을 한 번만 한다.

## 표기

코드블록 밖에서는 장식용 유니코드를 쓰지 않는다. 이모지, 곡선 따옴표, em대시,
화살표 기호, 박스드로잉, 체크와 엑스 표시가 여기 해당한다. 화살표는 `->` 로,
대시는 `--` 로, 체크와 엑스는 `OK` 와 `FAIL` 로 적는다.

코드블록 안은 예외다. 실제 출력과 실제 코드를 고치지 않고 그대로 옮긴다. 규칙에
맞춰 고쳐 적으면 문서가 실제와 달라지기 때문이다.

## 문서

한 사실은 한 곳에만 적는다. 다른 곳에서는 링크로 가리킨다. 요약본을 두지 않는다.
그 사실이 바뀔 때 두 곳 이상을 고쳐야 하면 위반이다. 번호, 파일 경로, 이름 같은
식별자가 여러 곳에 나오는 것은 가리키는 것이므로 위반이 아니다. 문장이 그대로
겹치는 것은 검사 C12 가 판정하고, 표현이 다른 중복은 사람이 판정한다.

산문 요약은 원본과 글자가 달라 기계로 일치를 확인할 수 없다. 손으로 맞추면
어긋난다.

강조를 위해 커스텀 태그로 내용을 다시 정의하지 않는다. 대문자 XML 태그가 있으면
위반이고 검사 C8 이 판정한다. 태그로 다시 정의하면 산문과 태그 안에 같은 내용이
두 벌 생긴다.

한국어 표현이 문법에 맞는지 확인한다. 조사가 앞 글자의 받침과 맞는지, 어색한
표현이 없는지 사람이 본다. 받침 규칙만으로는 어절 끝 글자가 조사인지 단어의
일부인지 가릴 수 없어 기계가 판정하지 못한다.

## 브랜치

`main` 에 직접 커밋하지 않는다. 브랜치를 만들고 `main` 으로 PR 을 만든다.

브랜치 이름은 첫 커밋을 한 날이다. 접두어를 붙이지 않는다.

```
2026-09-01
```

모든 브랜치가 같은 접두어를 쓰면 그 접두어는 아무것도 구분하지 않는다.

브랜치를 만든 날에 커밋하지 못했으면 커밋 전에 이름을 그 커밋의 날짜로 바꾼다.
푸시하기 전이므로 이름만 바꾸면 된다.

브랜치는 한 번에 하나만 연다. 손대고 있는 브랜치와 열려 있는 PR 을 합쳐 하나다.
저장소가 막는 것이 아니라 한 번에 한 갈래로 작업하기로 정한 것이다.

머지는 merge commit 으로 한다. merge commit 메시지에 브랜치 이름과 PR 번호가
남아서 브랜치를 지운 뒤에도 어느 브랜치였는지 찾을 수 있다. squash 와 rebase 는
그 이름을 남기지 않고, squash 는 나눠서 만든 커밋도 하나로 합친다. 저장소 설정에서
그 둘을 꺼 두면 규칙과 실제 동작이 어긋날 수 없다.

에이전트는 PR 을 만드는 데까지 한다. 머지하지 않는다. 사용자가 머지를 지시하면
검토하고 진행한다.

머지한 브랜치는 지운다. 같은 날 또 필요하면 같은 이름으로 다시 만든다.

## 커밋

사용자가 명시적으로 지시하기 전에는 커밋하지 않는다. 푸시도 같다.

커밋 메시지 본문에 검토한 항목과 승인 사실을 적는다. 이것이 사용자가 승인했다는
흔적이다.

## 버전

스킬 작업이 끝나 사용자 승인을 받은 커밋에서 올린다. 중간 커밋마다 올리지
않는다. 매니페스트 사이의 일치는 스킬 규격 S6 이 정하고 검사가 판정한다.

```
patch   표현 수정, 오타, 근거 보강. 규칙과 절차가 바뀌지 않는다
minor   규칙이나 절차가 바뀐다. 스킬이 추가된다
major   스킬이 제거된다. 디렉토리 구조가 바뀌어 기존 설치가 깨진다
```

기준은 쓰는 쪽이 무엇을 다시 봐야 하는가다. patch 는 안 봐도 되고, minor 는
달라진 규칙을 봐야 하고, major 는 설치를 다시 해야 한다.

## 검사 스킵

기본은 스킵하지 않는다. 스킵하려면 사용자 승인을 받고 커밋 메시지에 이유를 적는다.

```
pre-commit   git commit --no-verify
CI           커밋 메시지에 [skip ci]
```

스킬 작성 절차의 검사 단계는 스킵하지 않는다.

## 작업 기록

아르마리움에 대한 논의와 결정은 저장소 밖에 남긴다.

```
/home/olbbemi/.local/share/armarium/
```

구조와 원칙은 `INDEX.md` 에 있다. 다음 셋을 지킨다.

1. `INDEX.md` 이외의 작업 기록을 읽기 전과, 저장 위치를 제안하거나 정하거나 파일을
   만들고 고치기 직전에 `INDEX.md` 를 읽는다. 내용이나 배치를 판단하지 않고 경로만
   인용하는 경우는 제외한다
2. 저장 위치를 정할 때 디렉토리 이름만 보지 않고 `INDEX.md` 의 적격 조건을 하나씩
   판정한다. 조건의 내용을 여기에 다시 적지 않는다
3. 실행 중 판단이 앞서 제안하거나 합의한 방식과 달라지면 멈추고 변경 이유와 새
   방식을 사용자에게 알린다

"메모리" 는 하네스 내장 메모리를 가리킨다. 작업 기록과 다른 것이다.

## 검증 이력

한쪽 하네스가 만든 산출물은 다른 쪽 하네스가 검증한다. 결과는 `verifications/`
아래에 검증 단위마다 하나씩 회차 누적으로 남긴다. 어느 단위인지는 파일 이름이
아니라 `target` 이 정하고, 파일 경로는 `target` 기준 상대 경로로 적는다.

```json
{
  "target": "skills/skill-authoring",
  "verifications": [
    {
      "verifier": "codex",
      "at": "2026-08-31",
      "files": {
        "SKILL.md": "<sha256>",
        "requests.md": "<sha256>"
      },
      "items": { "T2": "pass", "T4": "fail" },
      "notes": "T4 -- description 마지막 문장이 절차를 요약한다"
    }
  ]
}
```

한 회차에는 그때 검증한 파일만 적는다. `verifier` 는 `claude` 또는 `codex` 다.
`items` 는 사람이 판정하는 규칙의 번호와 결과다. 해시는 sha256 으로 파일 내용을
그대로 계산한다.
